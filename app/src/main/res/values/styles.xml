<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/av_color_primary</item>
        <item name="colorPrimaryDark">@color/av_color_primaryDark</item>
        <item name="colorAccent">@color/av_color_accent</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>

</resources>
